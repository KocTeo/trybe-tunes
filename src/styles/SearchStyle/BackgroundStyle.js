import styled from 'styled-components';

const BackgroundStyle = styled.div`
    height: 100em;
    background: linear-gradient(180deg, #02223D, white);
    box-shadow: inset 0px 4px 4px rgba(0, 0, 0, 0.25);

    #result-title {
        margin-left: 230px;
        color: #C5FFFD;
    }
`;

export default BackgroundStyle;
